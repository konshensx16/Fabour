{% extends 'base-logged-out.html.twig' %}

{% block title %}Sing in to your account!{% endblock %}

{% block body %}

    <div class="d-md-flex flex-row-reverse">
        <div class="signin-right">

            <div class="signin-box">
                <h2 class="signin-title-primary">Welcome back!</h2>
                <h3 class="signin-title-secondary">Sign in to continue.</h3>
                {% for message in app.flashes('success') %}
                    <div class="alert alert-success mg-t-20">
                        {{ message }}
                    </div>
                {% endfor %}

                {% if error %}
                    <span class="error">{{ error.messageKey|trans }}</span>
                {% endif %}

                {{ form_start(form) }}
                    {{ form_widget(form._username, {'attr': {'value': last_username}}) }}
                {{ form_end(form) }}
                <div class="signup-separator"><span>Or sign in using</span></div>
                <a href="{{ path('hwi_oauth_service_redirect', {'service': 'facebook' }) }}" class="btn btn-facebook btn-block">
                    <span>Facebook</span>
                </a>

                <a href="{{ path('hwi_oauth_service_redirect', {'service': 'google' }) }}" class="btn btn-google btn-block">
                    <span>Google</span>
                </a>

                <a href="{{ path('hwi_oauth_service_redirect', {'service': 'twitter' }) }}" class="btn btn-twitter btn-block">
                    <span>Twitter</span>
                </a>

                <div class="signup-separator mt-3"><span>Other options</span></div>

                <p class="mg-b-0">Forgot your password? <a href="{{ path('security.reset.password') }}">Reset the password</a></p>
                <p class="mg-b-0">Don't have an account? <a href="{{ path('register.createAccount') }}">Sign Up</a></p>
            </div>

        </div><!-- signin-right -->
        <div class="signin-left">
            <div class="signin-box">
                <h2 class="slim-logo"><a href="index.html">slim<span>.</span></a></h2>

                <p>We are excited to launch our new company and product Slim. After being featured in too many magazines to mention and having created an online stir, we know that ThemePixels is going to be big. We also hope to win Startup Fictional Business of the Year this year.</p>

                <p>Browse our site and see for yourself why you need Slim.</p>

                <p><a href="" class="btn btn-outline-secondary pd-x-25">Learn More</a></p>

                <p class="tx-12">&copy; Copyright 2018. All Rights Reserved.</p>
            </div>
        </div><!-- signin-left -->
    </div><!-- d-flex -->

{% endblock %}